plugin_configs:
    -
        id: 1
        plugins:
            custom-signature-authorizer:
                body: "custom-signature-authorizer plugin is working."
routes:
    - id: 1
      uri: /*
      plugin_config_id: 1
      upstream:
        nodes:
          "httpbin.org:80": 1
        type: roundrobin
#END